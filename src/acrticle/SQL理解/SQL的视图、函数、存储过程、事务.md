---
title: SQL的视图、函数、存储过程、事务
icon: database
order: 4
category:
  - SQL理解
tag:
  - SQL基础
  - 数据库查询
---

# 视图

视图就是一个 `SELECT` 语句，是一个虚拟表。

# 函数

函数：存储在数据库中；可以把它当成计算器，主要用于计算。

# 存储过程

存储过程：存储在数据库里面；预定义了一些 SQL 语句。

# 事务

事务：不存储在数据库里面；包裹着一些 SQL 语句，并且只能全部执行成功，否则会回滚到执行前的状态。
